[ai_timer,lord_iban_boss_controller]
// do stuff

//def_int $random_spawn = random(100);

if (%npc_int = 100) {
   %npc_int = 0;
   npc_say("Spawning a random chicken!");
   npc_add(npc_coord, chicken, 100);
} else {
  %npc_int = add(%npc_int, 10);
}

